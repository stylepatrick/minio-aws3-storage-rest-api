spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 100MB
      max-request-size: 100MB
      file-size-threshold: 100MB

server:
  port: 8080

minio:
  bucket: bucket
  defaultFolder: test
  user: minio
  pwd: minio123
  url: http://127.0.0.1:9000