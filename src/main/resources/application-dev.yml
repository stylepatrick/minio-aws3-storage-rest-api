minio:
  bucket.name: bucket
  default:
    folder: test
  access:
    name: minio
    secret: minio123
  url: http://127.0.0.1:9000
server:
  port: 8080
spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 100MB
      max-request-size: 100MB
      file-size-threshold: 100MB